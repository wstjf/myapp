version: 2.1

executors:
  android-flutter:
    docker:
      - image: gmemstr/flutter-fastlane:latest
    environment:
      TERM: dumb
      _JAVA_OPTIONS: "-Xmx2048m -XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap"
      GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx2048m"'
jobs:
  beta_deploy:
    executor: android-flutter
    steps:
      - checkout
      - run: export codehome=$PWD
      - run: cd /home/cirrus/sdks/flutter
      - run: git remote add origin https://github.com/flutter/flutter
      - run: flutter upgrade
      - run: cd $codehome && flutter build apk --target-platform android-arm64

workflows:
  deploy:
    jobs:
      - beta_deploy:
          filters:
            branches:
              only: master